# TASK-003 统一知识/数学检查入口

- 任务：一条命令运行 L0 知识 lint 与五项 L1 Node 数学检查。
- 命令：`powershell -ExecutionPolicy Bypass -File CUDA_coding/scripts/check_all.ps1`。
- 结果：61 个项目 Markdown、21 个规范 Wiki、8 个来源登记、0 个 lint 错误。
- 警告：3 个既有 Wiki 页面共缺少 5 项 `confidence/source_ids` 扩展元数据，已登记 Q-010；核心 `id/type/status` 均存在。
- 数学验证：penta 独立直解、28 槽通信、hepta 独立直解、一般 `m` 结构、一般递推五项全部退出码 0。
- 修正：首次运行发现 Windows PowerShell 5.1 对无 BOM UTF-8 中文路径的默认解码问题；脚本已显式使用 UTF-8，并避免在 `.ps1` 源码中嵌入中文路径字面量。
- 边界：本入口不覆盖 NVHPC 编译、CUDA kernel、MPI request、设备通信或真实多 GPU 性能。
- 状态：TASK-003 `done`。
