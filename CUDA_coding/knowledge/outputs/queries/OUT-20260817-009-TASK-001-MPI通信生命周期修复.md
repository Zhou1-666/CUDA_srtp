---
id: OUT-20260817-009
type: query-output
status: compiled
created: 2026-08-17
source_ids: [SRC-003P, SRC-003T]
wiki_pages_read: [KB-BRIEF-PENTA, KB-SYSTEM-002, KB-TEST-001]
compiled_to: [KB-BRIEF-PENTA, KB-SYSTEM-002, KB-TEST-001]
---

# TASK-001 MPI 通信生命周期修复

## 问题与边界

修复三对角和五对角 `pascal_a2av` 在阻塞式 `MPI_ALLTOALLV` 后等待未初始化 request 的错误。只允许最小通信正确性修复和必要的 MPI 返回码检查；禁止修改消元算法、28 槽布局、性能路径和 benchmark。

## 改动前基线

从仓库根目录运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File CUDA_coding/scripts/check_all.ps1
```

退出码为 0：L0 检查 63 个 Markdown、21 个 Wiki、8 个来源、0 error、5 个既有扩展元数据 warning；五项 L1 Node 数学检查全部通过。

## 代码修复

- 三对角：删除未初始化的 `requestA/requestB`、无效的两个 `MPI_WAITALL` 和对应的废弃注释代码；检查 `MPI_ALLTOALLV` 返回码，失败时打印错误码并 `MPI_ABORT`。
- 五对角：删除同样的 request/WAITALL；保持 CUDA-aware 与 host staging 分支不变；只有 host collective 成功才把 `hB` 回写 device `B`；统一检查返回码并在失败时中止。
- 未改动消元、28 槽、solver/profile 调用结构、计时和 benchmark。

静态子程序检查结果：三对角 `MPI_ALLTOALLV=1`、五对角 `MPI_ALLTOALLV=2`；两者 `MPI_WAITALL=0`、request 数组引用为 0，且均有 `MPI_SUCCESS` 与 `MPI_ABORT` 路径。`git diff --check` 通过。

## 改动后检查

再次运行同一统一检查，退出码为 0；L0 仍为 0 error、5 warning，五项 L1 数学检查全部通过。该结果只证明知识库一致性和数学脚本未回归，不证明 CUDA Fortran/MPI 二进制正确。

## 目标环境阻断

当前 Windows PowerShell 中 `nvfortran`、`mpif90`、`mpiexec`、`make` 均未找到，无法执行 L2 干净构建、L3 单 rank 或 L4 多 rank 回归。

本阶段状态当时为 **blocked——代码修复完成、目标环境验证待完成**。该状态已由后续目标环境验证取代。

## 目标环境复验

在 Linux/WSL/HPC 的 NVHPC + MPI 环境，从源码根目录执行：

```bash
bash hpc/check_env.sh | tee env_check.txt
make veryclean
make all 2>&1 | tee build_all.log
mpirun -np 1 ./run/a.out | tee tri_np1.log
mpirun -np 2 ./run/a.out | tee tri_np2.log
mpirun -np 1 ./run/a.out_penta | tee penta_np1.log
mpirun -np 2 ./run/a.out_penta | tee penta_np2.log
```

验收要求：干净构建退出码 0；所有 rank 正常结束；无 hang、MPI/CUDA error、NaN/Inf；保存环境、构建与四份运行日志。完成并审查这些证据后，才能重新判定 TASK-001 状态。

## 后续状态

2026-08-17 已在 WSL2 + NVHPC 24.11 + OpenMPI 4.1.5 完成干净构建及最终 `np=1/2` 三、五对角回归，TASK-001 更新为 `done`。完整证据见 `knowledge/outputs/validation/TASK-001-20260817-wsl-nvhpc24.11/README.md`。
