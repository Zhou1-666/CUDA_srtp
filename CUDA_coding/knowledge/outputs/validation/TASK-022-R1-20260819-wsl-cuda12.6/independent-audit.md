# TASK-022-R1 独立归档复核

日期：2026-08-19

审查模型：GPT-5.6-sol

推理强度：high

结论：`PASS（归档与重复性）；不替代原回执，不产生新的单一性能主数字`

## 独立核对

- 原始运行目录与本归档目录各有 13 个文件；逐文件 SHA-256 一致；
- 环境 SHA 为 `15123ce`；`f868ea9..15123ce` 间 `cusparse_penta_baseline.cu`、`Makefile.cusparse`、`run_task_022_cusparse.sh` 无差异；
- 两个 correctness 日志和两个独立 warmup 日志存在；
- `formal-runs.csv` 恰有五条数据行，均为 manufactured、`64×64×1024`、batch 4096、FP64、`iterations=10,warmups=0`；
- exact1/manufactured 最大解误差分别为 `1.1107e-11`/`1.0436e-11`，最大残差为 `2.7756e-15`/`2.6645e-15`，通过 `1e-10`；
- 从原始 CSV 独立重算：solver 中位数 `5.606400013 ms`、均值 `5.723256330 ms`、CV `3.857337%`、IQR `0.183091259 ms`、MAD `0.037487936 ms`，与 `formal-stats.txt` 一致。

## 结论边界

R1 证明 adapter 在相同硬件/版本/输入下可重复通过正确性 Gate，且本轮波动低于原公平回执。R1 与原回执的中位数不同，不能把较小值作为“更新后更快”的结论：两轮不是预先定义的同一联合样本，也没有与 TASK-014 在同一会话配对计时。

允许：把 R1 作为第二轮带完整原始数据的绝对计时回执，并在论文中并列披露 R0/R1 的统计。

禁止：覆盖或删除 R0；合并两轮后声称单一十样本结果；只选择 R1 支撑速度比；将任一 P=1 结果外推为多 GPU、普遍性能或算法最优。
